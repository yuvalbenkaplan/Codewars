//instructions: write a function that takes a number and returns its ooposite.
//example: if input is 1, return -1. If input is -14, return 14

function opposite(number) {
    return (number*(-1))
  }

  console.log(opposite(-20))